# 补充测试循环

仅当主代理工具列表中存在 `SendMessage` 工具时启用；否则主代理跳过本节，直接进入 SKILL.md 第 5 节。

文中 F = impl-agent、V = test-agent。

## 触发与基本结构

所有 F 完成报告后，主代理主动询问用户："是否要进行补充测试？" 用户同意才进入本流程。

**严格串行**：一个 F-V 对的测试循环完整闭环后，才进入下一对；任何时候只有一对在跑。

## 单对 F-V 循环

对于每个 F：

1. 主代理判断 F 的改动是否有必要测试。无需测试 → 该对通过，处理下一对。
2. 需要测试 → 主代理冷启动 V（`subagent_type: plan-then-delegate:test-agent`，前台串行），使用以下模板。V 会自行通过 SendMessage 找到 F 并完成修复-复验循环 —— 详见 test-agent 定义中的「与 F 通信」和 impl-agent 定义中的「测试循环中的角色」。
3. 主代理从 V 的 spawn 结果中获取 V 的 agent_id，发一条短消息给 V：`你的 agent_id 是 <V_id>，发给 F 时附上。`
4. 主代理等待 V 报告最终结论（"通过"或"卡点"）。

**主代理只做三件事**：起 V、告知 V 其 agent_id、收 V 的最终报告。不参与 F ↔ V 之间的来回。

## V 冷启动模板

（必填字段不可省，可选字段按需）

```
## 本轮修复代理 F 的改动范围  [必填]
- 修改文件：<file_list>
- 改动要点：<key_changes_from_F_report>

## F 的 agent_id  [必填]
<F 的 agent_id>

## 特殊测试命令  [可选]
<覆盖默认；通常不填>
```
