---
description: >
  plan-then-delegate 的 auto 模式。主代理通过 TeamCreate 团队模式并行分发多个实现子代理。
  任务拆分需做冲突检查。要求 TeamCreate 和 SendMessage 工具可用。
---

# Plan-Then-Delegate（auto 模式）

## 前置检查

主代理首先确认工具列表中存在 `TeamCreate` 和 `SendMessage`。缺任一则告知用户并停止。

## 核心原则

主代理只做两件事：**和用户对齐方案** + **整理文档**。具体代码改动通过插件内置 agent 落实，**通过 TeamCreate 团队模式并行分发**。

主代理**不**：
- 提前清理团队；进入第5节完成文档前（如不确定，主动询问用户），不能提前清理团队。
- 主动 poll/sleep，子代理完成消息会主动 push 到主代理
- 因为子代理 idle 通知，就提醒用户"子代理在线等待"
- 给子代理的落实细节提意见
- 自己接手问题和下场写代码改文件（除非用户明确要求"这次你直接改"，或者你已有所需的上下文，告知用户征得同意）
- 自己看子代理处理问题后的 log（应该把用户反馈的 log 文件路径直接丢给子代理让它自己看）
- 把同一个问题拆给多个子代理
- 让子代理来写文档
- 自己跑编译 / 测试验证（编译归 F，测试归 V；主代理不关心命令、不重复跑）

## 工作流

1. **方案对齐**（第 1 节）：主代理排查根因，与用户确认方案
2. **并行分发**（第 2 节）：冲突检查 → TeamCreate → 并行 spawn 多个 impl-agent → 等待全部返回 → TeamDelete
3. **收集反馈**（第 3 节）：主代理对每个子代理返回做轻量校验
4. **补充测试循环**（第 4 节）：所有 F 完成后询问用户；同意则进入 test-agent ↔ impl-agent 循环
5. **统一整理文档**（第 5 节）

测试循环保持串行（每对 F-V 轮流），不受 auto 模式影响。

### 1. 排查与方案对齐

主代理通过 Read / Grep / Glob / 必要的 WebSearch 等只读工具排查根因，把问题的"根因 + 拟定方案"呈现给用户，**等用户明确确认**后再分发。

不确定的点不要替用户拍板，也不能把猜测当作确定的方案擅自推进，更不能把未经用户确认的选项当作方案推进。用户可能对方案细节有偏好（命名、风格、范围），这一步把它们对齐清楚。

### 2. 并行分发

**保存 agent_id**：Agent 工具调用时必须传 `name` 参数（如 `F1`/`F2`），返回结果中包含 `agent_id`，主代理记录每个子代理的 name ↔ agent_id 对应关系。流程后续需用 `SendMessage(to: agent_id)` 续接同一个子代理——agent_id 跨"已退出"状态仍然有效；冷启动会丢上下文、token 翻倍。

#### 冲突检查（分发前必做）

拆分任务时确保并行 agent 间：
- 改动文件路径不重叠
- 改动逻辑无交叉
- 每个 agent prompt 中明确列出「不要碰的文件」

#### 分发流程

1. **TeamCreate**：创建团队（如 `ptd-auto`）
2. **并行 spawn**：一条消息中并发多个 Agent 调用（`subagent_type: plan-then-delegate:impl-agent`，`team_name: ptd-auto`），每个带完整 prompt 和 `name`
3. **等待全部返回**：进入第 3 节
4. **TeamDelete**：收集反馈后清理

#### prompt 模板

（必填字段不可省）

```
## 任务背景  [必填]
<一句话说明问题>

## 根因  [必填]
<主代理已排查的根因>

## 已确认方案  [必填]
- 改动文件：<file_path>
- 关键代码 / 函数：<片段或定义>
- 命名 / 风格：<用户特别强调的偏好>

## 不要碰的文件  [auto 模式必填]
- <其他子代理正在处理的文件>

## 特殊编译命令  [可选]
<覆盖默认；通常不填>
```

需要让实现子代理返工时，如果因为主代理工具列表中不存在 `SendMessage` 或其他原因，无法让原子代理继续，需要冷启动一个新的子代理接力。

一个问题对应一个实现子代理。
**禁止**存在处理过问题相同模块的实现子代理时，因为用户的提问有部份不确定同属一个模块，就偷懒打包，暴力新建子代理处理。
如果分析问题后发现，是之前某一个流程处理的后续或者同一模块（"上次改了 A 之后又发现 B，或者没完全改好"），同时主代理工具列表中存在 `SendMessage`，则分发给处理那个流程的实现子代理继续处理。
如果同一问题涉及多个文件 / 多步，全部在同一个实现子代理里完成，不要拆。
如果多个问题集中在同一模块或关联性大，不涉及有不同实现子代理处理过的部分，可以合并成一次修改任务。

### 3. 收集反馈并继续

子代理返回后，主代理只做轻量校验（看反馈里有没有偏离方案、有没有遗留点）。**不**重新读子代理改过的文件来"验证"——子代理 Edit/Write 失败会直接报错，没必要主代理重复确认；可以做的是必要时浏览关键改动确认理解。

如果反馈正常，起下一个实现子代理处理下一个问题；否则澄清问题，必要时向用户提问，然后调整让 prompt 只包含最新状况，让子代理返工，子代理返回结果后，重新进入第 3 节；每次返工阶段结束，确认一下当前流程。

### 4. 补充测试循环（可选）

**触发条件**：所有 F 完成报告后，**且主代理工具列表中存在 `SendMessage` 工具**。主代理主动询问用户："是否要进行补充测试？" 用户同意才进入本流程；用户不要、或 SendMessage 不可用，直接跳到第 5 节。

**核心结构**：每个 F 配一个 V（一一对应、严格串行，任何时候只有一对在跑）。主代理冷启动 V 时传入 F 的 agent_id，之后 F ↔ V 通过 `SendMessage` 直连——V 发现问题直接发给 F，F 修复后直接回 V 复验。主代理不参与中继，只等 V 的最终报告。每对完整闭环后才进入下一对。

**详细执行规则、SendMessage 用法、V 模板**：见插件 `skills/step/references/test-loop.md`。

### 5. 统一整理文档

所有子代理（含可选的测试循环）都跑完后，主代理询问用户，是否要把所有改动整合写到项目约定的汇总 / 长期文档里：
- 文档结构由项目 CLAUDE.md / README / 既有文档惯例决定；主代理顺着既有结构写，不强加形态
- **不重复跑编译 / 测试**——子代理已经自跑通过；主代理只整合内容
- 如果走了第 4 节测试循环，把测试结果一并记录

## 何时不用

- 单个简单改动（一行 typo、一个变量重命名）：主代理直接改更快
- 用户明确说"这次你直接改"："对齐方案 → 主代理落盘"
- 纯研究 / 探索任务（没有代码改动）：用 explore / general-purpose 子代理即可，不需要本技能的角色分工
- TeamCreate 或 SendMessage 不可用时改用 `/plan-then-delegate`
